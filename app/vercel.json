{
   "rewrites": [{ "source": "/(.*)", "destination": "/" }],
   "ignoreCommand": "if [ -z \"$VERCEL_GIT_TAG\" ]; then exit 0; fi; if git diff --quiet HEAD^ HEAD -- supabase/; then exit 0; else exit 1; fi"
}
